import "../css/serverLoader.css";

import Spinner from 'react-spinner-material';
import React, { Component } from 'react';
const ServerLoader = () => {
  return (
<div class="loader body-loader">Uploading</div>
 
  );
};

export default ServerLoader;
